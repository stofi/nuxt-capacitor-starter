import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Nuxt Capacitor Starter',
  webDir: '.output/public',
}

export default config
